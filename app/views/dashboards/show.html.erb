<div class='form-box'>
  <div class='row'>
    <div class='col-sm-12'>
    <h1>My Dashboard</h1>
     <h2> Classes I'm Taking</h2>
       <p>  
        <% current_user.enrolled_courses.each do |course| %>

           <h3 class="text-center">&para; <%=link_to course.title ,course_path(course) %></h3>

            <%= image_tag course.image, :class => 'img-responsive' %>
            <%= course.description %>
            <%= link_to "More Info...", course_path(course), :class => 'btn btn-lg btn-primary' %>
            <hr>
         <% end %> 
       </p>
       </div>
  </div>
   <div class='row'>
    <div class='col-sm-12'>
    <h2>Classes I'm Teaching</h2>
     <% current_user.courses.each do |course| %>
       <h3 class="text-center">
         &para; <%= link_to course.title, course_path(course) %>
       </h3>
   
       <%= image_tag course.image, :class => 'img-responsive' %>
       <%= course.description %>
   <%= link_to "More Info...", course_path(course), :class => 'btn btn-lg btn-primary' %>
   <hr>
     <% end %>
   </div>
 </div>
</div>